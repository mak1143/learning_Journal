[project]
name = "startup"
version = "0.1.0"
description = "Learning Journal — a small Django journal app"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.16",
    "django-bootstrap5>=26.3",
    "python-dotenv>=1.2.3",
]
